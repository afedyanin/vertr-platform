@using Vertr.MarketData.Contracts
@using Vertr.Platform.BlazorUI.Components.Models

@implements IDialogContentComponent<OpenPositionModel>

<FluentMessageBarProvider Section="@ModelConsts.MESSAGES_DIALOG" MaxMessageCount="1" />

<FluentDialogBody>
    <FluentDatePicker Label="Date" Disabled=_selectDateDisabled @bind-Value="@Content.SelectedDate" /><FluentTimePicker @bind-Value="@Content.SelectedTime" Disabled=_selectDateDisabled />

    <FluentSelect Label="Instrument"
        TOption="string"
        @bind-Value="@Content.InstrumentId">
        @foreach (var item in _instruments)
        {
            <FluentOption Value="@item.Id.ToString()" Selected=@(Content.InstrumentId != null && item.Id.ToString() == Content.InstrumentId)>@item.GetFullName()</FluentOption>
        }
    </FluentSelect>

    <FluentNumberField @bind-Value="@Content.QuantityLots" Appearance="FluentInputAppearance.Filled" style="width: 100%;">Qty lots:</FluentNumberField>
    <FluentNumberField @bind-Value="@Content.Price" Disabled=_selectPriceDisabled Appearance="FluentInputAppearance.Filled" style="width: 100%;">Price:</FluentNumberField>
</FluentDialogBody>
