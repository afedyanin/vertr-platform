version: '3.8'

services:
  tinvest:
    build:
      context: .
      dockerfile: Dockerfile.TinvestGateway
    ports:
      - "8080:8080"
    networks:
      - infra_default

networks:
  infra_default:
    external: true
    driver: bridge